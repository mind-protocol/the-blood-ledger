# Schema Archive Notes (2024-12)

## Summary

- Archived verbose narrative role/dynamics blocks previously embedded in `docs/schema/SCHEMA.md`.
- Removed duplicate full node/link listings from `docs/schema/SCHEMA_Moments.md` in favor of concise, split schema files.
- Current canonical schema now lives in `docs/schema/SCHEMA/` and `docs/schema/SCHEMA_Moments/`.

## Rationale

The module exceeded the size threshold. Content was consolidated into concise schema references and split into focused files.
